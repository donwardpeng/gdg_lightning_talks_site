import 'package:flutter/material.dart';
import 'main.dart';

class Session {
  String speaker_name;
  String bio;
  String headshot;
  String session_title;
  String session_description;
  Session(this.speaker_name, this.bio, this.headshot, this.session_title, this.session_description);

}

